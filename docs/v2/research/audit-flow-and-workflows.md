# Setup-flow + workflows audits (2026-09-23)

## Setup flow (verified: zip writes only manifest.json, VERSION, SETUP.md, bundle.toggles.md, README.txt, PASTE-TO-CLAUDE.md — bind.jsx:187-255 / index.html:1772-1818)
- Zip has no _core/, no top folder; paste brief points at ./claude-code-templates/SETUP.md (doesn't exist) -> abort. Preview tree/file counts are fabricated (buildZipManifestPreview).
- Manifest toggles "on/off/ask" vs SETUP expects true/false/null. Versions disagree (v1.3.0 manifest, v1.4.0 VERSION, v1.1.0 README). Two different install commit messages. SETUP refs retired START-HERE.html.
- Useless decisions: Manual vs Discovery; tool slots ci/ai_reviewer/issue_tracker are dead (no template consumes them); 36 tri-state toggles; cleanup question.
- Silent failures: overwrites CLAUDE.md/CHANGELOG/CONTRIBUTING outside Discovery; leftover precommit/, *.template, all 7 architecture/*.md (all load!); install PR edits claude-code-review.yml -> OIDC red on first PR; fork PRs get no secrets -> gate fails closed on every OSS contribution; token setup hand-waved; statusline/time hook never installed but close skill depends on [time].
- Toggles: configurator 36, TOGGLES.md 31 (5 missing, 2 default mismatches). Dead: billable_handoff_summary, team_handoff_notes, branch_protection_loose/strict, binary_verdict_rule. Same-in-all-bundles -> bake in or drop. Bundles = 2 questions (solo/team x own/client). Target: 2 questions + <=7 advanced switches (AI review, deep auto-escalation, code-research hook, precommit, UI rule, confidentiality, clean-room).
- SETUP.md 54KB: ~15KB hook-install edge cases for custom tools, ~8KB mechanical substitution, ~4KB legacy aliases; marker grammar duplicated 3x (SETUP, TOGGLES, buildSetupMd fallback).
- Deterministic bind: page renders _core in-browser (~40 lines regex), zip = ready overlay + install-global.py; Claude only does TAILOR.md (<=5KB judgment work). Cut custom code-research tool + ctags/semgrep/sourcegraph.
- Skills: add from downstream: board/issue source of truth (toggle), scheduled-CI-failure check, ahead/behind, propose branch; close: stop conditions, cascade, context-file branch rule, named git add, --body-file, visual gate, Resolves #N, tokensave commit/pr_context, measured adherence. Canonical still ships sleep 420 loops; hardcoded Gitflow. find 12KB pre-bind; architecture-graph OFF everywhere -> add-on; ritual duplicated CLAUDE.md vs skills.

## Workflows + hooks
Port (general): triage fail-closed Case 0 (CAN auto-passes when triage dies = fail-open hole, EMB 1130-1139); paginated files API not gh pr diff (406 >300 files); local git diff for prescreen/reviewer; deterministic escalation enforced (EMB 571-619, 1001-1068); sticky needs-deep-review label; --disallowedTools Agent,Task,ScheduleWakeup,SendMessage,ListAgents + "work alone"; Fable 5.1 low primary + Opus 5.5 medium fallback; deep failure classifier incl plan-cap text; tiered verdict extraction; "ERRORED not a finding" status; action pin 7ada98c1 (v1.0.213) + token via env; never inline ${{ }} in long run blocks (21k cap); ready_for_review trigger; secret-literal scan; "run analyzer, reviewer trusts it" pattern.
DO NOT PORT: EMB/STK dropped claude[bot] author filter on verdict lookup (verified; both repos PRIVATE so low risk; Emberholm session notified FYI). CAN keeps filter.
Simplify: one .github/scripts/review_gate.py (check/verdict/escalation/classify-failure) + pytest fixtures; .github/review-gate.yml config + repo vars for models -> workflow files byte-identical for every user (no placeholders, no OIDC trap on tuning). Bigger: single workflow triage->review->deep->gate(always()) with ONE required check "Review gate"; manual @claude advisory. Hole: PR can rewrite gate job -> CODEOWNERS .github/** + code-owner review, or gate via pull_request_target.
Hooks: ship .claude/hooks/*.py registered in COMMITTED .claude/settings.json ($CLAUDE_PROJECT_DIR, python shim). settings.local.json.template is gitignored downstream -> cloud/teammates never see it. Ship no-ai-attribution (generic), block-gh-run-watch, block-claude-workflow-off-main (gitflow toggle); pr-body-contract optional; skip issue-body-contract, tokensave-first (tokensave installer owns its hook). attribution {"commit":"","pr":""} in committed settings.json. CI prescreen scans commit messages + PR body for attribution (catches web/cloud).
Setup friction: 8 manual steps (GitHub App, setup-token secret, workflows on default branch, needs-deep-review label, rulesets, merge methods, placeholders, reload). Private repos on Free plan: rulesets unavailable -> gate silently advisory. Proposal: idempotent scripts/setup-review-gate.sh (labels, merge settings, rulesets via API, secret, App detect, plan detect) + --check mode from session-start.
