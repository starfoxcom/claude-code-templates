# Rules audit (2026-09-23) - summary
Port back: Monitor CI watch (replace 420s/90s loops in C/token-efficiency L298-344, git.md L238-250); never rerun --failed (orphans Claude On-Demand); precise post-merge cleanup (canonical "--delete-branch only handles remote" is wrong); --body-file + post-merge addCloseIssueReferences (toggle, 10-link cap); why workflow changes land on main (S/git.md L123-139 table); pre-code scope analysis; dead code does not ship (+ DI/reflection blind spot); review gate refinements (3-condition escalation, literal "@claude review", deep label blocks routine-only merge, fail-closed verdict); move gate internals to lazy docs; model pins only via best-of-3 A/B; drop usage-cost table; MCP hygiene; visual: one checklist per slice, silence is not an override, record smoke-test date; clean-room abbreviation grep; fix-everywhere after CI finding; labels only when used.
New canonical: task-tracking.md (always-on ~25 lines); shipped-text.md (toggle, paths: user-facing, per-language word lists); code-size.md (language profile, allowlist + ratchet); testing.md (paths: source); paths: frontmatter scoping mechanism (biggest token win; canonical scopes 0 rules); UI extras into visual.md; rules_audit HOT/WARM/COLD behind lazy_rules_folder.
Cut/merge: binary verdict duplicated (git.md vs review-tiers); tier table dup; local role dup; fast path dup; cascade snippet x3. CONTRADICTIONS: collaboration.md L272 "squash on merge" vs never squash; L283 "don't merge own PR" vs admin fast path. Stale "Sonnet"/"Opus" hardcoded -> {{REVIEW_*_MODEL}}. Hook-enforced bans -> one line + ship hooks in _core/global-template/hooks/ (only code-research-first today). Stale: "read verdict comment" section, jq blockquote, cost table, state machine eager.
Target: eager ~200 lines (git 85, review-tiers 55, workflow 45, task-tracking 25) down from ~490; toggle/path-scoped: visual, testing, code-size, shipped-text, clean-room, collaboration, confidentiality, architecture. Lazy: docs/ci-review-architecture.md. One owner per concept.
